# see glib-genmarshal(1) for a detailed description of the file format,
# possible parameter types are:
#   VOID        indicates   no   return   type,  or  no  extra
#               parameters. if VOID is used as  the  parameter
#               list, no additional parameters may be present.
#   BOOLEAN     for boolean types (gboolean)
#   CHAR        for signed char types (gchar)
#   UCHAR       for unsigned char types (guchar)
#   INT         for signed integer types (gint)
#   UINT        for unsigned integer types (guint)
#   LONG        for signed long integer types (glong)
#   ULONG       for unsigned long integer types (gulong)
#   ENUM        for enumeration types (gint)
#   FLAGS       for flag enumeration types (guint)
#   FLOAT       for single-precision float types (gfloat)
#   DOUBLE      for double-precision float types (gdouble)
#   STRING      for string types (gchar*)
#   PARAM       for GParamSpec or derived types  (GParamSpec*)
#   BOXED       for boxed (anonymous but reference counted) types (GBoxed*)
#   POINTER     for anonymous pointer types (gpointer)
#   OBJECT      for GObject or derived types (GObject*)
#   NONE        deprecated alias for VOID
#   BOOL        deprecated alias for BOOLEAN

BOOLEAN:BOOLEAN
BOOLEAN:STRING
BOOLEAN:BOXED
BOOLEAN:BOXED,POINTER,POINTER,POINTER
BOOLEAN:UINT
BOOLEAN:UINT,POINTER
BOOLEAN:UINT,OBJECT,OBJECT
BOOLEAN:UINT,POINTER,POINTER
BOOLEAN:UINT,UINT
BOOLEAN:UINT,UINT,BOXED
BOOLEAN:UINT,UINT,INT
BOOLEAN:UINT,UINT,LONG
BOOLEAN:UINT,UINT,POINTER
BOOLEAN:UINT,UINT,UINT,BOXED
BOOLEAN:UINT,UINT,UINT,UINT
BOOLEAN:UINT,UINT,UINT,UINT,UINT,STRING
BOOLEAN:UINT,UINT,UINT,ULONG,BOXED
BOOLEAN:UINT,UINT,ULONG
BOOLEAN:UINT,UINT,ULONG,ULONG
BOOLEAN:UINT,UINT,ULONG,ULONG,ULONG
BOOLEAN:VOID
OBJECT:BOXED
STRING:VOID
VOID:BOXED
VOID:OBJECT
VOID:STRING
VOID:VOID
